from setuptools import setup

setup(
    name="deepcolor",
    version="0.0.1",
    description="Colorize a black and white image",
    packages=["deepcolor"],
    install_requires=["Pillow"],
)
